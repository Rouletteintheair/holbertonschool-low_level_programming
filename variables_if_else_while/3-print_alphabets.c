#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main-Enrty main
 * Return:Always 0 (Success)
 */
int main(void)
{
	char i;
	char o;
	char p;

	for (i = 'a'; i <= 'z'; i++);
	for (o = 'A'; o <= 'Z'; i++);
	
	{
		putchar(i);
		putchar(o);
	}
	putchar('\n');

	return (0);
}
